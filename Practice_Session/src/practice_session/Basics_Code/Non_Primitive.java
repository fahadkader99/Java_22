package practice_session.Basics_Code;

public class Non_Primitive {
    public static void main(String[] args) {

        /* Non-Primitive data types;
        1. this types of data are created by programmers and they are not pre-defined.
        2. premitive data has avalue while non-premitive data don't have any value
        3. Premitive starts with lower case, but Non-premitive starts with Uppercase letter.

        Examples: Strings, Arrays, Classes, Interfaces etc.
         */


        // How to swap the values of 2 variables;

//        int x = 100;
//       int  y = 10;
//
//       int temp = x;
//       x = y;
//       y = temp;


        /* Ternary operators gets the name from having 3 operands:
    EX: exp1 ? exp2 : exp3
        Boolean bool = 2 > 1 ? true : false;

        x++ -> unary [single] operand;
        int x = 1; int y = 2; x = x + y; -> binary;
        x > y -> binary;


         */


//        String pass = "Pass";
//        String fail = "Fail";
//
//        int testResult = 73;
//
//        String passOrFail = testResult > 50 ? pass : fail;
//        System.out.println(passOrFail);

            System.out.println( 'j' + 'a' + 'v' + 'a');
    }
}
