package march_23;

import java.util.Arrays;
import java.util.Scanner;

public class RemoveElementsArr {
    // find an element from array
    public static void main(String[] args) {

        /*
        // set the array
        Scanner sc = new Scanner(System.in);
        Scanner scanner = new Scanner(System.in);
        System.out.println("Enter the size of array: ");
        int size = sc.nextInt();
        String[] originalArr = new String[size];

        // build the array
        for (int i = 0; i< originalArr.length; i++){
            System.out.println("Enter element " + (i+1));
            //originalArr[i] = sc.nextInt();
        }
        System.out.println("\nArray: " + Arrays.toString(originalArr));

        // Num to delete
        System.out.println("How many element wants to Remove ?");
        int elementsToRemove = sc.nextInt();
        String[] elementRemove = new String[elementsToRemove];

        // Elements to delete in another array
        for (int i = 0; i<elementRemove.length; i++){
            System.out.println("Enter elements "+ (i+1));
            elementRemove[i] = sc.nextLine().toUpperCase().trim();
        }

        // after removed new array
        String [] afterRemoved = new String[originalArr.length - elementsToRemove];


        // compare if contains or not
        for (int i = 0; i < originalArr.length; i++ ){
            if (!originalArr[i].equalsIgnoreCase(elementRemove[i])){

            }
        }

    */






        /**
         * when Remove element from array - size should decrease & other element should move to lower index & return a new array
         */
    }
}
